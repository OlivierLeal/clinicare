<nav class="bg-blue-600 h-20 flex items-center justify-between px-6 text-white">
    <div class="flex space-x-4">
        <%= link_to 'Home', root_path, class: "text-lg font-semibold hover:text-blue-200" %>
        <% if user_signed_in? %>
            <%= link_to 'Chamados', calls_path, class: "text-lg font-semibold hover:text-blue-200" %>
        <% end %>
    </div>
    <div>
        <% if user_signed_in? %>
            <%= link_to 'Deslogar', destroy_user_session_path, data: { turbo_method: :delete }, class: "text-lg font-semibold hover:text-blue-200" %>
        <% else %>
            <%= link_to 'Login', new_user_session_path, class: "text-lg font-semibold hover:text-blue-200" %>
        <% end %>
    </div>
</nav>
<hr class="border-gray-300">
